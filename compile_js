#!/bin/bash
for i in *.js; do
    namesp=$(echo $i | sed -e 's/.js$//g' | sed -e 's/_/ /g')
    echo -n "<p><a href='javascript:"
    ./UglifyJS/bin/uglifyjs -mt -nc -c $i
    echo -n "'>$namesp</a>"
    [ -e $i.readme ] && (echo ": "; cat $i.readme)
    echo "</p>"
done > install.html
